# 운영 문서 (OPERATIONS.md)

이 서비스는 배포 후 방치되면 위험해지는 지점이 있다.
그래서 **가능한 점검은 전부 자동화**하고, 사람이 해야 하는 일만 여기에 남겼다.
인수인계 시 이 문서 하나만 넘기면 되도록 모든 절차를 여기에 모은다.

> 이 문서는 PLAN.md Phase 4.5의 운영 체크리스트를 실제 구현에 맞춰 옮긴 것이다.
> 정책의 단일 소스는 여전히 `PLAN.md`와 `taxonomy.yaml`이다.
> 두 파일과 이 문서가 어긋나면 **PLAN.md / taxonomy.yaml이 우선**이며, 이 문서를 고친다.

---

## 1. 시스템 구성 한눈에

```
GitHub Actions (매일 1회, PT 자정 직후)
  ├─ scripts/build_videos.py    taxonomy.yaml → YouTube API → videos.json
  └─ scripts/check_channels.py  channel_allowlist.yaml → channel_health.json
         │
         ▼ gh-pages 브랜치 data/ 하위에 배포 (keep_files: true)
   data/version.json        수십 바이트 — 앱이 먼저 받아 버전 비교
   data/videos.json         본체 — 변경됐을 때만 받는다
   data/channel_health.json 채널 점검 결과
         │
         ▼
   PWA (앱은 YouTube API를 절대 직접 호출하지 않는다)
```

**API 키는 GitHub Actions Secrets(`YOUTUBE_API_KEY`)에만 존재한다.**
앱 번들·저장소·videos.json 어디에도 키가 들어가지 않는다.

| 파일 | 역할 | 누가 고치나 |
|---|---|---|
| `taxonomy.yaml` | 감정 체계·검색어·blocklist 계층·위기 정책의 단일 소스 | 운영자 (PR) |
| `channel_allowlist.yaml` | 위기 카테고리 채널 화이트리스트 — "이 채널만 쓴다" | 운영자 (PR) |
| `channel_blocklist.yaml` | 채널 차단 목록 (일반+위기 전부) — "이 채널은 쓰지 않는다" | 운영자 (PR) |
| `scripts/build_videos.py` | 배치 본체 | 개발자 |
| `scripts/check_channels.py` | 채널 건강 점검 | 개발자 |
| `.github/workflows/build.yml` | 스케줄·배포·Issue 자동 생성 | 개발자 |

---

## 2. 정기 운영 체크리스트

| 주기 | 항목 | 방법 | 담당 |
|---|---|---|---|
| 매일(자동) | 배치 성공 여부 | 실패 시 Actions가 Issue 자동 생성 (`[배치] 데이터 파이프라인 실패`) | 시스템 |
| 매일(자동) | 채널 생존·업로드·필터 통과율 | `channel_health.json` 생성, 이상 시 Issue 자동 생성 | 시스템 |
| 매일(자동) | 위기 풀 신선도 | `crisis.updated_at`이 3일 이상 멈추면 Issue 자동 생성 | 시스템 |
| 월 1회 | 위기 카테고리 영상 샘플 점검 | 실제 노출 영상 10개를 **사람이 직접 열어** 확인 | 운영자 |
| 월 1회 | 나쁜 채널 발견 시 차단 | `channel_blocklist.yaml`에 추가 (4.5절 절차) | 운영자 |
| 월 1회 | 자동 생성된 Issue 처리 | 채널 교체·제외 후 `channel_allowlist.yaml` 수정 | 운영자 |
| 분기 1회 | 화이트리스트 전체 재검토 | 채널별 최근 영상 성향 변화 확인, 필요 시 신규 채널 보충 | 운영자 |
| 분기 1회 | `blocklist_tiers` 갱신 | 신조어·새로운 위험 표현 반영 | 운영자 |
| 분기 1회 | **앱 번들 시드 갱신** | `app/src/data/seed-videos.json`을 최신 `videos.json`으로 교체 (아래 절차) | 운영자 |
| 반기 1회 | 감정 키워드 사전 보강 | 오분류 사례 반영 | 운영자 |

### 앱 번들 시드 갱신 — 구체적 절차

`app/src/data/seed-videos.json`은 앱에 함께 묶여 나가는 **최초 실행용 영상 목록**이다.
다음 세 경우에 이 파일이 화면에 그대로 나온다.

- 앱을 처음 열었을 때 (아직 아무것도 받지 않은 상태)
- 오프라인에서 처음 열었을 때
- 캐시가 30일을 넘겨 폐기됐을 때 (YouTube API 약관상 30일 내 갱신 의무)

**방치하면 위험해지는 지점이 여기다.** 시드가 낡을수록 위 세 경우에 나오는 영상 중
삭제·비공개된 것이 늘어난다. 앱이 썸네일 404를 감지해 카드를 조용히 숨기므로 화면이
깨지지는 않지만, 목록이 점점 비어 보인다. 특히 **위기 카테고리의 시드가 비면
상담 안내 아래에 아무것도 남지 않는다.**

```bash
# 1. 최신 배치 산출물을 받는다 (또는 직접 배치를 1회 실행한다)
curl -o dist/videos.json https://appleap385-aaz0919.github.io/FindYourMind/data/videos.json

# 2. 시드로 복사한다 (minify — 번들 크기를 줄인다)
python -c "import json; d=json.load(open('dist/videos.json',encoding='utf-8')); \
  open('app/src/data/seed-videos.json','w',encoding='utf-8').write(json.dumps(d,ensure_ascii=False,separators=(',',':')))"

# 3. 위기 영상이 들어 있는지 반드시 확인한다
cd app && npm test
```

`npm test`의 "crisis 영상과 일반 카테고리 영상이 서로소다", "모든 세분류에서 categories
조회 결과에 위기 영상이 섞이지 않는다"가 시드를 대상으로 돌기 때문에, 잘못된 파일을
넣으면 여기서 걸린다. 통과하면 커밋한다 (변경 추적은 4절 참조).

> 시드는 `dist/`가 아니라 `app/src/data/`에 있다. `dist/`는 `.gitignore` 대상이라
> CI 체크아웃에 존재하지 않으므로, 번들에 들어가려면 추적되는 경로에 있어야 한다.

### 월 1회 위기 카테고리 샘플 점검 — 구체적 절차

1. `data/videos.json`의 `crisis.videos`에서 무작위 10건을 고른다.
2. `https://youtube.com/watch?v={videoId}`로 **직접 열어본다.**
   자동 필터는 **제목만** 본다. 설명·태그·썸네일과 실제 내용은 사람만 판단할 수 있다.
3. 아래에 하나라도 해당하면 그 채널을 화이트리스트에서 제외한다.
   - 감정을 몰입·증폭시키는 연출 (자극적 배경음, 눈물 유도 자막)
   - 자기계발·성취 압박 톤
   - 댓글이 열려 있고 위험한 내용이 달려 있음
4. 점검 결과를 그달의 Issue에 코멘트로 남긴다 (누가 언제 봤는지 흔적을 남긴다).

---

## 3. 자동 생성 Issue 대응 가이드

배치는 **판정만** 하고 Issue 생성은 워크플로가 한다.
같은 사유의 Issue가 이미 열려 있으면 새로 만들지 않고, 일정 기간 조용했을 때만 코멘트를 단다.
**Issue를 닫는 것이 곧 "처리 완료" 표시다.**

| 재알림 주기 | 대상 | 왜 |
|---|---|---|
| **1일** | `crisis_empty` | 위기 영상 0건은 "낡았다"가 아니라 **안전 장치가 없다**는 뜻이다. 매일 알린다 |
| **2일** | `crisis_stale`, `allowlist_undersized` | 안전 관련. `crisis_stale`은 3일에 발동하는데 재알림이 7일이면 위기 풀이 낡아가는 상태를 주 1회만 알리게 된다. 감지 주기보다 알림 주기가 길면 경보가 무의미하다 |
| 7일 | 나머지 전부 | 매일 코멘트가 쌓이면 오히려 읽지 않게 된다 |

주기는 `scripts/lib/alerts.py`의 경보 정의에 `renotify_days`로 붙어 있다.
워크플로는 그 값을 읽기만 하므로, 주기를 바꾸려면 그 파일 한 곳만 고치면 된다.

| Issue 제목 | 원인 | 조치 |
|---|---|---|
| `[화이트리스트] 채널 접근 불가` | 채널 삭제·비공개·차단 | 채널 상태 확인 → `channel_allowlist.yaml`에서 제외하거나 교체 (PR) |
| `[화이트리스트] 90일 무업로드` | 채널 활동 중단 | 신선도 문제. 대체 채널을 보충하고 필요하면 제외 |
| `[화이트리스트] blocklist 통과율 저하` | **채널 성향이 변했다는 신호** | 최근 업로드를 직접 확인. 부적합하면 즉시 제외 |
| `[화이트리스트] 재검토 기한 초과` | `last_reviewed_at` 120일 경과 | 채널 확인 후 `last_reviewed_at`·`reviewed_by` 갱신 (PR) |
| `[화이트리스트] 채널 수 부족` | 활성 채널 15개 미만 | 선정 기준에 맞는 채널 보충. **풀 고갈 직전 신호** |
| `[화이트리스트] 예시 항목이 남아 있음` | 스캐폴드 `UC_EXAMPLE_...` 미교체 | 실제 채널로 교체하고 예시 항목 삭제 |
| `[위기 카테고리] 확보량 미달 — 직전 결과 유지` | 필터 통과분 12건 미만 | 화이트리스트 보충. **필터를 완화하지 말 것** |
| `[위기 카테고리] 풀이 갱신되지 않음` | `crisis.updated_at` 3일 이상 정지 | 위 상황이 반복 중이라는 뜻. 화이트리스트를 실제로 채워야 한다 |
| `[위기 카테고리] 영상 0건` | 신규 확보 실패 + 유지할 직전 결과도 없음 | 앱은 상담 안내만 표시(의도된 안전 동작). 최우선 처리 |
| `[배치] 카테고리 확보량 미달` | 특정 세분류 15건 미만 | 검색어 적합성 확인 (`taxonomy.yaml`) |
| `[배치] blocklist 과다 필터링` | 후보의 50% 이상이 blocklist에 걸림 | 용어가 과한지 검색어가 부적절한지 판단. 계층별 건수가 본문에 있다 |
| `[배치] 데이터 파이프라인 실패` | 배치 자체 실패 | 실행 로그 확인. 직전 videos.json은 그대로 유지된 상태 |

### 절대 하지 말 것

- **위기 카테고리 영상이 부족하다고 blocklist를 완화하지 않는다.**
  12건 미달 시 직전 결과를 유지하는 것이 설계된 동작이다.
  영상이 적은 것은 문제가 아니고, 검증 안 된 영상이 노출되는 것이 문제다.
- **`channel_allowlist.yaml`을 직접 푸시하지 않는다.** 반드시 PR로.
- **필터 통과율이 낮은 채널을 "일시적이겠지" 하고 두지 않는다.** 성향 변화는 되돌아오지 않는다.

---

## 4. 화이트리스트 관리 절차 (누락 방지 장치)

1. 채널은 `channel_allowlist.yaml` **한 파일에서만** 관리한다. 다른 곳에 흩어두지 않는다.
2. 채널 추가·제외는 **추적 가능한 형태로** 한다 — 아래 "변경 추적" 참조.
3. 각 채널 항목에 `added_at`, `last_reviewed_at`, `reviewed_by`를 **필수로** 기록한다.
   (하나라도 비면 배치가 실패한다. 조용히 넘어가지 않는다.)
4. 배치가 `last_reviewed_at` 120일 초과 채널을 발견하면 Issue를 자동 생성한다.
5. 화이트리스트가 15개 미만으로 줄면 배치가 경고 Issue를 생성한다 (풀 고갈 방지).
6. 인수인계 시 이 문서 하나만 넘기면 되도록 모든 절차를 여기에 모은다.

### 변경 추적

2026-08-18 이전에는 2번이 "PR로만 한다"였다. 1인 운영에서는 자기 PR을 자기가
머지하는 형식만 남고, 정작 지키려던 것 — 누가 왜 바꿨는지 추적 — 은 PR이 아니라
아래 세 가지가 지킨다. **문서가 지켜지지 않는 규칙을 담고 있으면 나머지 규칙의
무게도 떨어진다.**

담당자가 1명인 동안 (현재)

| 무엇을 | 어디에 |
|---|---|
| 추가한 채널의 검토 근거 | 그 채널 항목의 `note` (검토일·검토자·판단 이유) |
| **제외한** 채널의 사유 | `channel_allowlist.yaml` 주석. 다음에 같은 채널이 후보로 올라와도 검토를 반복하지 않게 한다 |
| 채널명과 검토 근거 | **커밋 메시지.** "화이트리스트 보충" 같은 요약만으로는 나중에 되짚을 수 없다 |

main 직접 push를 허용한다.

담당자가 2명 이상이 되면 **PR 필수로 전환한다.** 그때부터는 리뷰가 형식이 아니라
실제 두 번째 눈이 된다. 이 전환은 잊기 쉬우므로, 담당자가 늘어나는 시점에 이 절을
다시 읽는다.

### 채널을 추가하는 실제 순서

**후보를 처음부터 찾아야 한다면** 수집 도구를 먼저 돌린다 (아래 "후보 수집 도구" 참조).
이미 넣을 채널을 알고 있다면 2번부터 시작한다.

1. **선정 기준 확인** (`taxonomy.yaml` `selection_criteria`)
   - 이완·명상·자연 소리·수면 등 정적인 콘텐츠를 꾸준히 올리는 채널
   - 자극적 썸네일·낚시성 제목을 쓰지 않는 채널
   - 공신력 있는 정신건강 기관·전문가 채널
2. **채널 ID를 확보한다.** 핸들(`@name`)이나 URL이 아니라 `UC`로 시작하는 24자 ID여야 한다.
   채널 페이지 → 공유 → 채널 ID 복사.
3. **최근 업로드 10건을 직접 훑는다.** 하나라도 blocklist에 걸릴 성격이면 넣지 않는다.
4. `channel_allowlist.yaml`에 6개 필드를 모두 채워 항목을 추가한다.
5. 로컬에서 확인한다.
   ```bash
   python scripts/check_channels.py --dry-run
   ```
6. PR을 올린다. 머지되면 다음 배치부터 반영된다.

### 후보 수집 도구 (`scripts/suggest_channels.py`)

**일회성 도구다. 배치·워크플로에 넣지 않는다.** 채널은 한 번 정하면 거의 바뀌지 않아
매일 돌 이유가 없고, 결과는 사람이 읽고 판단해야 하는 검토 시트다.
정기 점검은 `check_channels.py`가 맡는다.

```bash
python scripts/suggest_channels.py --top 30 --reviewer <내-아이디>
```

위기 전용 검색어 6개로 검색해 자주 등장하는 채널을 집계하고, 각 채널의 구독자 수·
최근 업로드일·blocklist 통과율·최근 영상 10건 제목을 모아 `channel_candidates.md`를 만든다.
문서 맨 아래에 `channel_allowlist.yaml`에 그대로 붙여넣을 YAML 블록이 함께 나온다.

쿼터는 `--top 30` 기준 약 661 units다. 배치(약 7,900)와 같은 날 돌려도 한도 안이지만
여유를 두려면 다른 날에 돌린다.

**자동 점검이 판단하는 것과 못 하는 것을 구분할 것.**

| 자동 점검이 보는 것 | 사람이 봐야 하는 것 |
|---|---|
| blocklist 통과율 | 썸네일이 자극적인지 |
| 90일 내 업로드 여부 | 낚시성 제목인지 |
| 최근 영상의 3분 미만 비율 | 정적인 콘텐츠를 꾸준히 올리는지 |
| | 기관·전문가 채널로서 공신력이 있는지 |

`자동 점검: 통과`는 **승인이 아니라 실격 사유가 없다는 뜻**이다.
붙여넣기 전에 각 항목의 `note`를 실제 판단 근거로 고쳐야 한다 — 생성된 `note`는
자동 수집된 사실일 뿐 선정 이유가 아니며, `# TODO(검토)` 주석이 붙어 나온다.

### 채널을 제외할 때

항목을 **삭제**한다 (주석 처리하지 않는다 — 주석은 나중에 실수로 되살아난다).
PR 본문에 제외 사유를 남긴다. 이력은 git이 기억한다.

---

## 4.5. 채널 차단 절차 (`channel_blocklist.yaml`)

화이트리스트의 반대 개념이다. 적용 범위가 다르니 헷갈리지 않는다.

| | 파일 | 적용 범위 | 의미 |
|---|---|---|---|
| 화이트리스트 | `channel_allowlist.yaml` | **위기 카테고리만** | 이 채널만 쓴다 |
| 블록리스트 | `channel_blocklist.yaml` | **일반 + 위기 전부** | 이 채널은 쓰지 않는다 |

### 언제 쓰나 — 제목으로 잡을 수 없을 때만

blocklist 용어를 추가해서 막을 수 있으면 **그쪽이 먼저다.** 채널 차단은 제목 필터로
구조적으로 잡히지 않는 경우에만 쓴다. 실제 사례:

```
"동창회에 남편을 데려갔는데, 선생님은 그를 보자마자 물컵을 떨어뜨리며…"
"내가 서울대에 합격하자, 새아버지는 나를 위해 갈비찜을 한 냄비…"
```

막장 가족 서사 낭독인데 blocklist 용어가 하나도 없다. 제목으로 잡으려면
"남편"·"합격" 같은 일상어를 넣어야 하고, 그러면 멀쩡한 영상이 대량으로 잘려나간다.
이럴 때 채널 단위로 막는다.

### 월 1회 샘플 점검에서 나쁜 채널을 발견했다면

**1. 정말 채널 단위여야 하는지 먼저 판단한다**

- 제목에 공통 용어가 있는가 → `taxonomy.yaml`의 `blocklist_tiers`에 용어를 추가한다
- 그 채널이 그 콘텐츠만 만드는가 → 채널 차단
- 가끔 괜찮은 영상도 올리는가 → 차단하지 않는다. 목록은 짧게 유지한다

**2. 채널 ID를 얻는다** (1 unit)

`videos.json`에는 채널명만 있고 ID가 없다. 문제 영상의 `videoId`로 역조회한다.

```bash
python - <<'PY'
import os, sys
sys.path.insert(0, "scripts")
from lib.quota import QuotaBudget
from lib.youtube import YouTubeClient
client = YouTubeClient(os.environ["YOUTUBE_API_KEY"], QuotaBudget(hard_cap=20))
for item in client.videos(["여기에_videoId"]):
    s = item["snippet"]
    print(s["channelTitle"], s["channelId"])
PY
```

**3. `channel_blocklist.yaml`에 추가하고 PR을 올린다**

```yaml
  - channel_id: UC...
    channel_name: 채널 이름
    added_at: "YYYY-MM-DD"
    added_by: 본인
    reason: >
      무엇이 문제인지, 그리고 **제목 필터로 왜 안 되는지**까지 적는다.
      다음 사람이 "이건 blocklist 용어로 되지 않나?" 하고 되돌리지 않게.
```

필수 필드가 하나라도 비면 배치가 실패한다.
변경 이유는 note에, 제외 결정은 주석에, 채널명과 근거는 커밋 메시지에 남긴다 (4절).

**4. 다음 배치 리포트에서 확인한다**

`build_report.json`의 각 카테고리 `filters.blocked_channels`에 채널별 차단 건수가 남는다.
0건이면 그 채널이 애초에 검색에 안 잡히고 있다는 뜻이니 목록에서 빼도 된다.

### 목록이 길어지면 검색어를 의심한다

채널 차단은 **두더지 잡기**다. 같은 장르 채널은 수천 개고, 하나를 막으면 다음이 들어온다.
목록이 빠르게 길어진다면 그건 **검색어가 그 장르를 부르고 있다는 신호**다.

실제로 "사연"·"이야기"가 들어간 검색어가 막장 서사를 대량으로 데려왔고,
검색어를 바꾼 것이 채널 차단보다 효과가 컸다. 채널을 더 막기 전에
`build_report.json`의 `queries`를 보고 어떤 검색어가 데려왔는지 먼저 확인한다.

---

## 5. 배포 구조

gh-pages 브랜치에 **앱(루트)과 데이터(`data/`)가 함께** 산다.

```
gh-pages/
├── index.html, assets/...      ← Phase 3 앱 (앱 배포 워크플로가 씀)
└── data/
    ├── version.json            ← 수십 바이트
    ├── videos.json             ← 본체
    └── channel_health.json
```

### 두 워크플로가 서로를 지우지 않게 하는 장치

| 장치 | 설정 | 왜 |
|---|---|---|
| `keep_files: true` | **양쪽 워크플로 모두** | 한쪽만 켜면 다른 쪽이 상대 파일을 지운다 |
| `concurrency.group: gh-pages-deploy` | **양쪽 워크플로 모두 같은 이름** | 동시 배포 시 브랜치 충돌 방지 |
| `cancel-in-progress: false` | 양쪽 | 배포 중 취소되면 브랜치가 반쯤 갱신된 채 남는다 |
| 배포 후 존재 확인 | 앱 워크플로 마지막 | `keep_files` 설정이 깨진 걸 조용히 넘기지 않는다 |

**최초 1회는 데이터 워크플로를 먼저 돌린다.** 앱 워크플로 마지막의 `data/` 생존 확인은
`data/videos.json`이 없으면 실패하는데, 데이터를 한 번도 배포하지 않은 상태에서는
당연히 없다. 순서를 지키지 않으면 첫 앱 배포가 실패한다 — 앱은 정상 배포됐고 검증만
걸린 것이므로 데이터 배치를 돌린 뒤 앱 워크플로를 재실행하면 된다.

```
1) Actions → "데이터 배치 (videos.json)" → Run workflow   (data/ 생성)
2) Actions → "앱 배포 (PWA)"             → Run workflow   (앱 + 검증 통과)
```

### 앱이 데이터를 받는 방식

매일 500KB를 받는 것은 낭비다. 앱은 **버전 파일을 먼저 받고, 바뀌었을 때만 본체를 받는다.**

```js
// GitHub Pages는 캐시 헤더를 제어할 수 없으므로 ?v= 쿼리로 무효화한다.
const meta = await fetch(`data/version.json?t=${Date.now()}`).then(r => r.json());
if (meta.version !== cachedVersion) {
  const data = await fetch(`data/videos.json?v=${meta.version}`).then(r => r.json());
  await saveToIndexedDB(data);
}
```

`version.json`에는 `version`과 `crisis_updated_at`만 들어 있다.
`crisis_updated_at`이 따로 있는 이유: 위기 풀이 "직전 결과 유지" 상태로 멈춰 있는지
앱과 운영자가 본체를 받지 않고도 알 수 있어야 하기 때문이다.

### Phase 3 앱 배포 워크플로에 반드시 넣을 것

> 아직 앱이 없어 워크플로 파일도 없다. Phase 3에서 만들 때 아래를 그대로 포함한다.

```yaml
concurrency:
  group: gh-pages-deploy      # 데이터 워크플로와 같은 이름이어야 한다
  cancel-in-progress: false

# ... 앱 빌드 ...

- name: gh-pages 배포
  uses: peaceiris/actions-gh-pages@v4
  with:
    github_token: ${{ secrets.GITHUB_TOKEN }}
    publish_dir: ./dist
    keep_files: true          # data/ 를 지우지 않는다

- name: 데이터 파일 생존 확인
  # keep_files 설정이 깨진 걸 조용히 넘기면 안 된다.
  run: |
    sleep 10
    for f in version.json videos.json; do
      url="https://<org>.github.io/<repo>/data/$f"
      code=$(curl -s -o /dev/null -w '%{http_code}' "$url")
      if [ "$code" != "200" ]; then
        echo "::error::data/$f 가 사라졌습니다 (HTTP $code). keep_files 설정을 확인하세요."
        exit 1
      fi
    done
    echo "데이터 파일 확인됨"
```

---

## 6. 쿼터 관리

YouTube Data API v3 일일 한도는 **10,000 units**이며 **태평양 시간 자정에 리셋**된다.

| 항목 | units |
|---|---|
| 일반 24 세분류 × 쿼리 3개(4개 중 로테이션) | 7,200 |
| 위기 전용 검색 6개 | 600 |
| 화이트리스트 채널 조회 (채널당 1) | ~21 |
| 영상 검증 `videos.list` | ~90 |
| 채널 건강 점검 | ~25 |
| **합계** | **≈ 7,936** |

- **하드캡 9,800**: 실행 전 예상 소모량을 계산해 넘으면 API를 한 번도 부르지 않고 중단한다.
- **검색어 로테이션**: 세분류마다 `skip = (day_of_year + subcategory_index) % 4`로
  매일 다른 쿼리 하나를 쉰다. 세분류마다 위상이 달라서 특정 성격의 쿼리가
  전 카테고리에서 동시에 빠지지 않는다. 4일이면 모든 검색어가 한 번씩 쓰인다.
- **`search.list(channelId=...)`는 절대 쓰지 않는다.** 채널당 100 units다.
  화이트리스트는 `channels.list` → uploads 재생목록 → `playlistItems.list`로 채널당 1 unit.

### 검색어를 추가하고 싶다면

세분류당 검색어를 4개에서 5개로 늘리면 하루 소모가 약 1,800 units 증가해 한도를 넘는다.
확장하려면 다음 중 하나를 먼저 정해야 한다.

1. 로테이션을 3/5로 조정 (여전히 세분류당 3쿼리 유지 → 소모 불변, 각 쿼리 주기만 길어짐)
2. 격일 분할 실행 (홀수날 12세분류 / 짝수날 12세분류)
3. 복수 프로젝트 키 순환 (약관 확인 필요)

어느 쪽이든 `scripts/lib/quota.py` 상단 주석의 예산표를 함께 갱신한다.

---

## 7. 장애 대응

### 종료 코드

| 코드 | 의미 | 워크플로 동작 |
|---|---|---|
| 0 | 성공 | 배포 진행 |
| 1 | 일반 오류 (네트워크·파싱 등) | 60초 후 **1회만** 재시도 |
| 2 | 쿼터 관련 중단 | **재시도하지 않음** — 다음 PT 자정 이후 재실행 |

재시도를 1회로 제한하는 이유: 실패한 실행도 쿼터를 이미 소모했다.
무한 재시도는 남은 예산을 갉아먹고 다음 날까지 영향을 준다.

### 배치가 실패하면 무슨 일이 일어나나

**아무 일도 일어나지 않는다.** 이것이 설계 의도다.

- 배치는 모든 처리가 끝난 뒤에야 파일을 쓴다 (`tmp` → `os.replace` 원자적 교체).
- 중간에 실패하면 부분 결과를 쓰지 않고, 워크플로는 배포 단계를 건너뛴다.
- 따라서 **직전 `videos.json`이 그대로 서비스된다.**
- 앱은 IndexedDB 캐시가 있어 데이터를 받지 못해도 동작한다.

반쯤 갱신된 파일이 배포되는 것이 가장 나쁜 실패 모드이고, 그 경로는 막혀 있다.

### 수동 재실행

Actions 탭 → `데이터 배치 (videos.json)` → **Run workflow**.
쿼터가 이미 소진된 날이라면 하드캡에 걸려 즉시 중단되므로 안전하다.

### 로컬에서 확인 (API 키 없이)

```bash
pip install -r scripts/requirements.txt
python scripts/build_videos.py --dry-run
python scripts/check_channels.py --dry-run
```

드라이런은 API를 호출하지 않고 결정적 가짜 데이터로 전 과정을 돈다.
가짜 데이터에는 3분 미만·blocklist 위반·비공개·중복 후보가 의도적으로 섞여 있어
각 필터가 실제로 걸러내는지 로그로 확인할 수 있다.
예상 쿼터 소모량도 실제 실행과 동일하게 출력된다.

특정 날짜의 검색어 조합을 확인하려면:

```bash
python scripts/build_videos.py --dry-run --day-of-year 200
```

### 실제 API를 소량으로 먼저 확인 (`--only`)

API 키를 새로 발급했거나 검색어를 손봤을 때, 하루치 7,900 units를 다 쓰기 전에
소량으로 실제 응답을 확인한다. 세분류 id를 쉼표로 지정하며 위기 카테고리는 `safety.crisis`다.

```bash
python scripts/build_videos.py --only anxiety.restless,safety.crisis
```

이 예시는 약 930 units만 쓴다(전체의 12%). 실행 전 예상 소모량이 항상 먼저 출력되므로
얼마가 나갈지 보고 나서 진행할 수 있다.

**부분 실행 결과는 배포되지 않는다.** 24개 카테고리가 다 들어 있지 않은 파일이
`videos.json` 이름으로 남으면 다음 배포 때 그대로 나갈 수 있기 때문이다.

| | 전체 실행 | `--only` 부분 실행 |
|---|---|---|
| 영상 데이터 | `videos.json` | `videos.partial.json` |
| 진단 리포트 | `build_report.json` | `build_report.partial.json` |
| `version.json` | 생성 | **생성 안 함** (배포 신호 자체를 만들지 않는다) |
| JSON 마커 | — | `"partial": true`, `"only": [...]` |

`--only`에 `safety.crisis`를 넣지 않으면 위기 카테고리를 건너뛰므로, 일반 카테고리에서
위기 videoId를 제외하지 않고 상호 배타 검증도 건너뛴다. 로그에 경고로 남는다.
데이터를 실제로 갱신할 때는 `--only` 없이 전체를 돌린다.

---

## 8. 최소 안전선

운영자가 장기간 부재해도 다음이 유지된다.

- `blocklist_tiers` 필터는 매 배치에서 그대로 적용된다.
- 위기 카테고리는 12건 미달 시 직전 결과를 유지한다 — 검증 안 된 영상이 새로 들어오지 않는다.
- 배치가 실패하면 직전 결과가 그대로 서비스된다.
- 위기 영상이 하나도 없으면 앱은 상담 안내만 표시한다.

**방치의 결과는 신선도 저하이지 안전성 저하가 아니다.**

다만 다음은 자동화가 대신해줄 수 없다.

- 썸네일·설명란·실제 영상 내용 판단 (필터는 제목만 본다)
- 채널 성향의 미묘한 변화 (통과율 50% 미만이 되어야 감지된다)
- 새로운 위험 표현·신조어 (`blocklist_tiers`에 사람이 넣어야 한다)

그래서 월 1회 샘플 점검과 분기 1회 재검토가 체크리스트에 남아 있다.
