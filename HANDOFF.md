# HANDOFF — 세션 인수인계

작성 2026-08-18 (KST 10:30) · 기준 커밋 `2e34055`

> 이 문서는 **다음 세션이 바로 일을 이어받기 위한 것**이다.
> 정책·구조·운영 절차는 각 원본에 있고 여기서 반복하지 않는다.
> 여기 적는 것은 **원본만 읽어서는 알 수 없는 것** — 왜 그렇게 됐는지, 지금 무엇이 걸려 있는지다.

---

## 1. 현재 상태

**배포 URL** https://appleap385-aaz0919.github.io/FindYourMind/
**저장소** https://github.com/appleap385-aaz0919/FindYourMind (main = `2e34055`, 로컬과 동기화됨)

| Phase | 상태 |
|---|---|
| 0 감정 체계 | 완료 (`taxonomy.yaml`) |
| 0.5 UI 프로토타입 | 완료 (`emotion-prototype.jsx` — 참조용, 실행 코드 아님) |
| 1 데이터 파이프라인 | **가동 중** — 매일 UTC 08:30 (KST 17:30) |
| 2 TF.js 모델 | **미착수** — 분류는 키워드 사전 단독 |
| 3 PWA 조립 | **배포 완료** — TF.js 연결만 Phase 2 대기 |
| 4 이후 | 미착수 |

**규모** (2026-08-18 기준)

```
감정 키워드   878개 / 세분류 24개 (최소 30개)
위기 키워드    55개
화이트리스트   16개 (목표 15~25) · 차단 채널 2개
실사용 문장 커버리지 98.6%
```

**테스트** — 전부 쿼터 0. 배치 전 사전 점검에서 5종이 돌고, 앱 배포에서 `npm test` 54개가 돈다.
어느 쪽이든 깨지면 배포되지 않는다.

```
scripts/quota_test.py      429/403 판정 18종 + 예약 로직 5종
scripts/normalize_test.py  분류 회귀 — 경계선·어간 폭·정상 동작
scripts/blocklist_test.py  채널 차단
scripts/spread_test.py     채널 분산 선정
scripts/coverage_test.py   실사용 문장 80개, 하한 80%
```

---

## 2. 이번 세션에서 한 일 — 판단 근거가 있는 것들

### 일반 카테고리 채널 편중 (커밋 `9f48927`)

한 채널이 20슬롯을 쓸어담고 있었다. 8/15 `anger.rage`는 힐링포유 12/20(60%),
8/14 `exhaustion.listless`는 소소한 일상 18/20(90%). **날마다 범인이 바뀌므로 채널 차단으로 풀 문제가 아니었다.**
원인은 `final = kept[:20]` — 상한이 아예 없었다. 배치 #7·#8에서 최다 점유 1/20, 전 카테고리 20채널로 해소.

> **회전은 위기에만 옳다.** 위기용 라운드로빈을 그대로 가져오며 일별 회전도 넣었는데, 검증에서 되돌렸다.
> 위기는 승인된 채널 10개라 슬롯(20)이 더 많아 회전이 "전원이 돌아가며 들어온다"지만,
> 일반은 채널이 50~80개라 "매일 다른 20채널 구간을 자른다"가 된다. 실측에서 day 40이면
> 관련성 상위 40채널이 통째로 빠졌다. **같은 장치도 맥락이 바뀌면 정반대로 작동한다.**

### 위기 화면 채널 중복 (커밋 `c5def8f`)

풀은 채널당 2건으로 잘 분산돼 있었지만 화면에는 4~6개만 나온다. 단순 셔플이라
정확 계산·20만 회 시뮬레이션 모두 **평균 48%** — 두 번에 한 번꼴로 같은 채널이 두 번 보였다.

### 사전 일괄 보강 577 → 878개 (`7d291de`, `25abf50`, `e39bbf9`)

제보를 하나씩 받아 고치는 방식의 한계가 드러나 일반 감정 어휘 목록을 참고해 한 번에 메웠다.
실사용 문장 커버리지 71.8% → 98.6%.

기존 키워드가 일상어를 삼키던 것 13개도 함께 정리했다(무료→무료 배송, 정체→교통 정체,
보고싶→영화 보고 싶어 등). **`여유`만 되살렸다** — `calm.ease`의 세분류 label이라 빼면
label 단독 입력이 분류되지 않는다. 제거 목록과 사유는 `taxonomy.yaml` 주석의 표에 있다.

### 위기 키워드 23 → 55개 + 두 원칙 명문화 (`a6fc92c`, `60ca1fb`, `2acae61`, `3204cd8`)

수치심 대분류 추가를 검토하다가 **더 급한 구멍**을 찾았다.

**[경계선 원칙]** 자책은 위기가 아니다. 자책 + 부담감이 위기다.
결과를 탓하는가("나 때문에 다 망했어" → 위기 아님), 존재를 탓하는가("나만 없으면 됐을 텐데" → 위기).
부담감 표현 8종이 통째로 빠져 있었다.

**[어간 원칙]** 위기 키워드는 자기 지향이 문법적으로 드러나야 한다.
`의미가없`이 **운영 중에** "이 회의는 의미가 없어"를 위기로 보내고 있었다 —
업무 얘기를 적은 사용자에게 상담 전화번호가 떴다. 경계선 원칙이 경고한 "위기 경로 희석"이
이미 일어나고 있던 것이다. 검사 절차를 문서화했다: 어간 앞에 사물·타인·장소를 주어로 붙여
문장을 만들어 보고, 자연스러우면 쓰지 않는다.

기존 45종에 같은 검사를 돌려 `세상에없`을 좁히고 `아무도날`을 외로움으로 옮겼다.
**`살기싫` 계열 4종은 일부러 두었다** — 좁혀 보니 "살기 싫어" 같은 가장 흔한 위기 표현이
전부 빠졌다. 오탐은 상담 안내가 뜨는 방향이고 좁히면 놓치는 방향이다.

### 수치심 대분류 — 검토 후 보류

쿼터는 문제가 아니었다(+605, 하드캡 이내). 보류 이유는 안전이다. 결정 기록과 재검토 조건을
`taxonomy.yaml`에 남겼다 — **"수치심이 없네?" 하고 그냥 추가하지 말 것.**

### 쿼터 예약 이중 계산 (`ed6195d`)

로컬은 Actions 소모를 못 봐서 배치 몫 7,900을 예약하는데, 그 판단이 로컬 기록만 봐서
**이미 써버린 몫을 또 예약**했다. KST 17:30부터 다음 날 16:00까지 하루의 대부분이 이 상태다.
`gh`로 그날 배치 성공 여부를 물어 예약을 푼다(쿼터 0). **모르면 예약한다** — gh 실패는 전부 None.

같이 발견: `suggest_channels.py`가 `check()`를 **import만 하고 부르지 않았다.**
`--daily-ceiling` 등 플래그가 아무 일도 하지 않는 상태였다.

### 화이트리스트 10 → 16개 (`f2c3d3d`)

`suggest_channels.py --top 50`(701 units) 결과에서 6개 선별. 제외한 4개도 사유와 함께 주석에 남겼다.
`allowlist_undersized` 경보가 해소된다.

### 문서 규칙 개정 (`2e34055`)

"채널 변경은 PR로만 한다"가 1인 운영에서 형식만 남아 있었다. 실질을 지키는 조건으로 바꿨다
(note·주석·커밋 메시지, 담당자 2명 이상이면 PR 필수 전환).

---

## 3. 미해결

| 항목 | 상태 | 다음 행동 |
|---|---|---|
| **육안 확인** | 미확인 (여러 세션째) | 사용자가 직접 한다. 화면 톤, 위기 화면, 오프라인 동작 |
| **Issue #4** | 오늘 밤 배치로 해소 예정 | 16채널 반영 확인 후 닫는다 |
| **누락 어휘 35개** | 후보만 추림 | 어간 폭 검증 필요 (아래 4절) |
| **`끝내고싶` 오탐** | 감수 중 | "이 일을 빨리 끝내고 싶어"가 위기로 간다. 재검토 조건은 taxonomy 주석 |
| **Phase 2 (TF.js)** | 미착수 | 문장형 9개가 판단 재료 (`coverage_test.py`) |

---

## 4. 다음에 할 일 (우선순위)

**1. 오늘 밤 배치 결과 확인** (KST 17:30 이후)

16채널 첫 적용이다. `build_report.json`에서:
- `crisis.channel_spread` — 16채널로 퍼지는지. 첫 바퀴 16건 + 둘째 바퀴 4건이면 상한 3에 걸리는 채널이 없어야 한다
- `alerts` — `allowlist_undersized`가 사라졌는지 → Issue #4 닫기
- 아티팩트로만 보관된다: `gh run download <id>`

**2. 누락 어휘 약 35개 어간 폭 검증** (쿼터 0)

참고 목록 대조에서 나온 후보 — 못마땅·언짢·약오르·속상·하찮·역겨·끔찍·메스꺼·혐오스러·
꼴보기싫·눈물겨·서운·애처로·북받치·주눅드·낙심·맥빠지·암담·안타까·고리타분·지긋지긋·
한탄·탄식·침통 등. **오탐 코퍼스로 재고 나서 넣는다.**
⛔ `분한`은 절대 넣지 말 것 — 차분하다·충분하다에 걸려 2026-08-14에 뺀 이력이 있다.

**3. 육안 확인** — 사용자 몫. 특히 위기 화면

**4. Phase 2 착수 판단** 또는 운영 관찰 계속

---

## 5. 주의사항

### 실제 API 실행은 반드시 사전 승인

`--dry-run` 없는 실행은 **명령어와 예상 쿼터를 제시하고 기다린다.**

### 쿼터

하루 10,000, 배치가 약 7,900. 리셋은 **PT 자정 = 여름 UTC 07:00 (KST 16:00)**.

- **"배치가 돌았다"와 "쿼터가 없다"는 다르다.** 배치 후에도 약 2,000이 남는다
- 로컬 `.quota_log.json`이 PT 날짜로 누적을 기록한다. Actions 소모는 여기 안 잡힌다
- 예약은 이제 `gh`로 배치 성공을 확인해 자동으로 풀린다 (`lib/actions_status.py`)
- 판단이 헷갈리면 `lib/quota_log.check()`를 직접 불러 숫자를 보고 말한다

### 안전 관련 변경

`safety.crisis_keywords`, `blocklist_tiers`, `channel_allowlist.yaml`, `channel_blocklist.yaml`은
안전 정책이다. 임의로 고치지 말고 근거와 함께 제안한다. `git push`도 사용자가 지시할 때만 한다.

---

## 6. 반복해서 배운 것

### 공짜 실행이 비싼 결과를 지운다

661 units로 만든 `channel_candidates.md`를 검증용 드라이런이 합성 데이터로 덮었다.
채널 ID가 사라져 다시 701 units를 써야 했다.

**드라이런은 "API를 안 부른다"는 뜻이지 "아무것도 안 건드린다"는 뜻이 아니다.**
산출물 경로가 같으면 공짜 실행이 비싼 결과를 지운다.

전 스크립트를 점검하니 **세 곳 모두 같은 함정**이었다. `build_videos.py`는 `--only`(partial)만
파일명이 갈려 있고 `--dry-run`은 갈리지 않아, `dist`에 드라이런하면 `videos.json`과
**`version.json`까지** 덮었다 — 앱이 "새 데이터가 있나"를 판단하는 파일이다.
셋 다 `.dry-run` 접미사로 분리했다.

### 근거 없는 추정을 사실처럼 보고하지 않는다

"전날 쿼터 소진 상태라 실행 불가"라고 단정했으나 실제로는 **2,086이 남아 있었다.**
사용자가 "어제 배치 후로 아무것도 안 했는데?"라고 되물어서 잡혔다.

판단을 보고할 때는 **근거를 함께 낸다** — 로그 내용, 날짜 계산, 코드가 실제로 무엇을 보는지.
"소진됐다" 같은 결론만 말하면 틀렸을 때 상대가 검증할 방법이 없다.
이 프로젝트에는 이미 그 근거를 뽑는 함수가 있었다(`quota_log.check()`). 부르지 않고 추측했다.

### 어간을 넓히는 것이 항상 안전한 쪽은 아니다

이 사전은 매칭 수가 같으면 **키워드 길이 합**으로 순위를 정한다.
그래서 `터질것같`(5자)을 넣으면 기존의 올바른 `웃음이`·`눈물이`·`속이터`(각 3자)를 전부 이겨
"웃음이 터질 것 같아"가 억눌림으로 갔다. **짧은 `터질`(2자)로 둬야 제자리로 간다.**

넓힐 때는 "무엇을 더 잡나"만 보지 말고 "무엇을 밀어내나"를 함께 본다.

### 고장 테스트는 고장이 실제로 주입됐는지부터 본다

회귀 테스트를 쓴 뒤 일부러 고장 내는 절차에서, **주입 자체가 실패한 것을 통과로 읽을 뻔한 일이
세 번** 있었다. 키워드가 줄바꿈으로 감겨 문자열 치환이 빗나가거나, 주석에도 같은 문자열이 있어
엉뚱한 곳을 바꿨다. 종료 코드 0을 보면 "고장이 안 잡혔다"가 아니라 **"고장이 들어가긴 했나"를 먼저 확인**한다.
파이프(`| tail`)를 쓰면 `$?`가 파이프 마지막 명령의 것이라는 함정도 같이 겪었다.

### 문서 규칙은 여러 파일에 흩어진다

"채널 변경은 PR로만 한다"가 **4개 파일 6곳**에 있었다(PLAN·OPERATIONS 3곳·README 2곳·
channel_allowlist 헤더). 한 곳만 고치면 문서끼리 어긋난다. **규칙을 바꿀 때는 전수 검색부터 한다.**
`OPERATIONS.md`는 "이 문서 하나만 넘기면 되도록"이 원칙이라 포인터가 아니라 전문을 넣어야 한다.

### 그 밖에 (이전 세션에서 이어짐)

- **검색어는 그 단어가 한국 유튜브에서 어떤 장르의 관용어인지 본다.** `사연`·`이야기`는 서사물을,
  `속마음`은 연애 타로를, `팟캐스트`는 어학 학습을 부른다
- **테스트가 실제 소스를 읽는지 확인한다.** 가짜 함수를 검사하면 통과하는데 기능은 망가져 있다
- **한글은 음절 단위다.** `미쳐버리`는 `미쳐버릴`을 못 잡고, `기쁘`는 `기쁜`을 못 잡는다
- **로컬에서 되는 것이 CI에서 안 될 수 있다.** CI 실패는 추측하지 말고 `gh run view <id> --log-failed`
- **필터를 넓힐 때는 반대 방향 피해를 먼저 계산한다**
- **증상이 보고된 위치가 원인의 위치가 아닐 수 있다.** 이번에도 그랬다 — "예약이 이상하다"의
  실제 원인은 예약 계산이 아니라 "로컬 기록이 Actions를 못 본다"였다

---

## 7. 새 세션에서 읽을 순서

1. **이 문서** — 지금 무엇이 걸려 있는지
2. **`taxonomy.yaml`의 `safety` 블록** — `[경계선 원칙]`과 `[어간 원칙]`.
   위기 키워드를 만질 일이 있으면 여기부터. 값만 보고 고치지 말 것
3. **`PLAN.md`** 1~4절 — 목표·제약·아키텍처·데이터 스키마
4. **`OPERATIONS.md`** — 운영 절차. 4절(화이트리스트·변경 추적), 6절(쿼터)
5. 코드를 만질 때만: `scripts/build_videos.py` 상단 docstring(실행 원칙 5가지) →
   `scripts/lib/` → `app/src/`

**빠른 확인 명령** (전부 쿼터 0)

```bash
python scripts/normalize_test.py                 # 분류 회귀 — 경계선·어간 폭·정상 동작
python scripts/coverage_test.py                  # 사전 커버리지 (하한 80%)
python scripts/coverage_test.py --compare c5def8f  # 일괄 보강 직전과 비교 (71.8% → 98.6%)
python scripts/spread_test.py                    # 채널 분산 선정
python scripts/blocklist_test.py                 # 채널 차단
python scripts/quota_test.py                     # 429/403 판정 + 예약 로직
python scripts/build_videos.py --dry-run --out-dir /tmp/x
cd app && npm test                               # 54개
```

> 드라이런은 이제 `.dry-run` 접미사로 분리되지만, `--out-dir`은 여전히 임시 경로를 주는 편이 안전하다.
