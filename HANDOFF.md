# HANDOFF — 세션 인수인계

작성 2026-08-16 (KST 14:12) · 기준 커밋 `c4b777b`

> 이 문서는 **다음 세션이 바로 일을 이어받기 위한 것**이다.
> 정책·구조·운영 절차는 각 원본에 있고 여기서 반복하지 않는다.
> 여기 적는 것은 **원본만 읽어서는 알 수 없는 것** — 왜 그렇게 됐는지, 지금 무엇이 걸려 있는지다.

---

## 1. 현재 상태

**배포 URL** https://appleap385-aaz0919.github.io/FindYourMind/
**저장소** https://github.com/appleap385-aaz0919/FindYourMind (main = `c4b777b`, 로컬과 동기화됨)

| Phase | 상태 |
|---|---|
| 0 감정 체계 | 완료 (`taxonomy.yaml`) |
| 0.5 UI 프로토타입 | 완료 (`emotion-prototype.jsx` — 참조용, 실행 코드 아님) |
| 1 데이터 파이프라인 | **가동 중** — 매일 UTC 08:30 cron |
| 2 TF.js 모델 | **미착수** — 모델 산출물 없음. 분류는 키워드 사전 단독 |
| 3 PWA 조립 | **배포 완료** — 단 TF.js 연결은 Phase 2 대기로 빠져 있음 |
| 4 이후 | 미착수 |

**자동으로 도는 것**

- 데이터 배치 (`build.yml`) — 매일 UTC 08:30, 약 7,900 units. gh-pages `data/`에 배포
- 앱 배포 (`deploy-app.yml`) — `app/**`·`taxonomy.yaml` push 시. gh-pages 루트에 배포
- 두 워크플로가 같은 브랜치에 쓴다. `keep_files: true` + 같은 concurrency 그룹으로 공존 (실증 완료)
- 경보 → GitHub Issue 자동 생성·중복 억제

**테스트** — 배치 전 사전 점검(API 호출 없음)에서 `quota_test` · `blocklist_test` · `normalize_test`가 돌고,
앱 배포에서 `npm test`(49개)가 돈다. 어느 쪽이든 깨지면 배포되지 않는다.

---

## 2. 이번 세션에서 고친 것 — 판단 근거가 있는 것들

원본 파일 주석에도 근거를 남겼지만, **왜 그 결론에 도달했는지의 맥락**은 여기에만 있다.

### "차분해"가 분노로 분류되던 오분류

키워드 `분해`(anger.unfair)가 **차분해·따분해·충분해**에 부분 매칭됐다. `분하`도 차분하다·충분하다에 걸린다.
둘 다 제거하고 `원통`으로 대체했다. 억울·부당·불공평·서러워가 이미 있어 회수 손실은 작고,
**평온한 입력을 분노로 읽는 쪽이 훨씬 나쁘다**는 판단이다.

> 교훈: 짧은 키워드는 일상어를 삼킨다. 추가 전에 "이 문자열이 다른 단어 안에 들어가는가"를 본다.

### 자해 표현이 격분으로 갈 뻔한 구멍

`anger.rage`에 `죽여버리·죽이고싶·없애버리고싶`을 넣자, **"나를 죽이고 싶어"가 격분으로 분류**됐다.
기존 위기 키워드 어디에도 안 걸렸기 때문이다(그전에는 미매칭이라 조용했다).
자해를 말한 사람에게 분노 완화 영상을 내미는 것이 이 서비스가 낼 수 있는 최악의 오분류다.

`safety.crisis_keywords`에 자기 지향 가드 5종(`나를죽이·나죽이고싶·나를죽여·나죽여버리·나를없애`)을 넣어
**순서로 해결**했다 — 위기 검사가 감정 분류보다 먼저 돌기 때문이다.

경계 사례: "그 사람이 나를 죽이려고 해"(위협받는 상황)도 위기로 잡힌다.
상담 안내를 불필요하게 보여주는 방향의 오탐이며, 자해를 놓치는 것보다 낫다고 판단했다.

### 막장 사연 유입 — 검색어가 장르를 부른다

세 번 반복된 같은 문제다.

| 검색어 | 실제로 온 것 |
|---|---|
| "속 시원해지는 사연 읽기" | 컬투쇼 사연 몰아보기, 시월드 복수극 |
| "따뜻한 라디오 사연" | "1살때 날 버린 친엄마…" 신청사연 |
| "마음 어루만지는 이야기" | Emotional Road 막장 가족 서사 |

`사연`·`이야기`처럼 **서사를 요구하는 단어**가 들어가면 그 장르가 통째로 온다.
"따뜻한"을 붙여도 소용없다.

### "속마음"은 연애 타로의 관용어다

대체 검색어로 "속마음 정리하는 시간"을 넣었더니 **상위 5건 중 4건이 타로·재회 콘텐츠**였다.
한국 유튜브에서 "속마음"은 사실상 연애 타로 전용어다. "감정 일기 쓰는 법"으로 바꿨다.
같은 이유로 "말하지 못한 마음 정리"도 후보에서 뺐다.

### 429로 오는 일일 쿼터 소진

일일 쿼터 소진이 403이 아니라 **429**로 왔다. 403만 보던 판정이 놓쳐서 60초 뒤 재시도하고 똑같이 실패했다.
상태 코드로는 가를 수 없다 — 같은 429가 분당 한도일 때도 있다.
**메시지의 limit 문자열**(`per day` vs `per minute`)로 가른다. `scripts/quota_test.py`에 응답 형태 18종을 고정했다.

### 테스트가 통과하는데 기능이 망가져 있던 일

"다시 적기" 수정 후 테스트 통과를 보고했는데 실제로는 깨져 있었다.
테스트가 **가짜 함수를 만들어 검사**하고 App.jsx를 한 줄도 읽지 않았기 때문이다.
원인은 `reset(nextMode = "text")`를 `onClick={reset}`로 넘긴 것 — React가 이벤트를 첫 인자로 주는데
기본값은 `undefined`일 때만 적용된다. 지금은 App.jsx 원문의 시그니처와 핸들러 배선을 직접 검사한다.

### CI에서 테스트가 아예 실행되지 않던 일

`node --test "test/**/*.test.js"` — glob 지원이 Node 버전에 따라 갈린다.
로컬 Node 24는 되고 CI Node 20은 "Could not find" 로 실패했다. **앱 배포가 5회 연속 실패한 진짜 원인**이었다.
버전 가정에 기대지 않도록 파일을 하나씩 적는 방식으로 바꿨고, 새 테스트 파일이 목록에서 빠지면 실패하는 검사를 넣었다.

### 그 외

- 로테이션 버그 — 검색어가 5개 이상이면 마지막 항목이 1년 내내 한 번도 안 쓰였다
- 위기 채널 편중 — 20건을 2채널이 독식했다. 라운드로빈 + 채널당 상한 3 + 일별 회전으로 10채널 균등 분산
- blocklist 매칭 범위 — 설명란까지 훑어서 **자살예방 상담전화를 적어둔 채널이 걸러지는 역설**이 있었다. 제목만 검사로 변경
- 중복 Issue — 같은 경보가 두 산출물에 담겨 한 실행에서 Issue가 2개 생성됐다

---

## 3. 미해결

| 항목 | 상태 | 다음 행동 |
|---|---|---|
| **홈 화면 아이콘** | 새 아이콘 배포 완료, **사용자 확인 대기** | 안 바뀌면 홈 화면에서 삭제 후 재추가. 그래도 안 되면 iOS 요청 크기(120·152·167) 추가 검토 |
| **화이트리스트 10/15** | Issue #4 (유일하게 열린 것) | `suggest_channels.py` 실행 승인받음 (661 units). 결과는 사람이 검토해야 하는 시트 |
| **배치 결과 확인** | KST 17:30 배치 대기 | 검색어 교체·tier_b 7종·채널차단 첫 적용. 아래 4절 참조 |
| **브라우저 육안 확인** | 미확인 | 화면 톤, 위기 화면, 오프라인 동작, service worker 썸네일 LRU |
| **Phase 2 (TF.js)** | 미착수 | 분류는 키워드 단독. `classify.js`에 모델 끼울 자리는 열어둠 |

> **육안 확인이 계속 밀려 있다.** 자동 테스트로는 잡히지 않는 영역(톤·레이아웃·오프라인 체감)이고,
> 이 세션에서는 브라우저 확장이 연결되지 않아 한 번도 보지 못했다.

---

## 4. 다음에 할 일 (우선순위)

**1. 배치 결과 확인** (KST 17:30 이후, 쿼터 0)

`build_report.json`의 `sadness.sorrow`에서:
- `filters.blocked_channels` — 채널별 차단 건수. **0건이면** 검색어 교체만으로 유입이 끊긴 것이니 `channel_blocklist.yaml`에서 빼도 된다
- `filters.dropped_by_tier` — tier_b 7종 효과
- `count` — 20건을 채웠는지 (드라이런 기준 후보 131 → 확보 81이라 여유 있음)
- `frustration.suppressed`·`sadness.lonely` — 사연 제거 후 확보량 (이전 측정 시 각 20건 확보)

**2. 화이트리스트 보충** — `suggest_channels.py` (661 units, 승인됨)

**3. 육안 확인** — 특히 위기 화면. "나를 죽이고 싶어" → 상담 안내가 최상단에 뜨는지

**4. Phase 2 착수** 또는 운영 관찰 계속

---

## 5. 주의사항

### 실제 API 실행은 반드시 사전 승인

`--dry-run` 없는 실행은 **명령어와 예상 쿼터를 제시하고 기다린다.**
2026-08-14에 확인 없이 측정용으로 돌린 실행이 그날 쿼터를 소진시켜 이후 작업이 전부 막혔다.

### 쿼터

하루 10,000, 전체 배치가 7,900. **사실상 하루 한 번밖에 못 돈다.**
리셋은 **PT 자정 = 여름 UTC 07:00 (KST 16:00)**. cron 배치는 UTC 08:30이라 리셋 직후다.

- 로컬 `.quota_log.json`이 날짜별 누적을 기록하고 (누적+예상+Actions 예약)이 9,500을 넘으면 실행을 거부한다
- **Actions 소모는 로컬 기록에 잡히지 않는다.** 러너가 매번 초기화되므로, 배치 몫 7,900을 예약으로 미리 빼둔다
- 그래서 **로컬 전체 배치는 기본 거부**된다. 의도한 동작이다 (`--no-reserve-actions-batch`로 해제)
- 실행 가능 시각: KST 16:00 이후가 새 쿼터 날. 배치(17:30)가 끝난 뒤가 가장 안전

자세한 수치는 `scripts/lib/quota.py` 상단.

### 안전 관련 변경

`safety.crisis_keywords`, `blocklist_tiers`, `channel_allowlist.yaml`, `channel_blocklist.yaml`은
안전 정책이다. 임의로 고치지 말고 근거와 함께 제안한다.
`git push`도 사용자가 지시할 때만 한다.

---

## 6. 반복해서 배운 것

**검색어를 고를 때 그 단어가 한국 유튜브에서 어떤 장르의 관용어인지 본다.**
사전적 의미가 아니라 실제 검색 결과가 기준이다. `사연`·`이야기`는 서사물을,
`속마음`은 연애 타로를, `팟캐스트`는 어학 학습을 부른다. 새 검색어는 실제로 돌려보고 상위 결과를 확인한다.

**테스트가 실제 소스를 읽는지 확인한다.**
가짜 함수를 만들어 검사하면 통과하는데 기능은 망가져 있다. 회귀 테스트를 쓴 뒤에는
**일부러 고장 내서 실패하는지** 확인하고 보고한다. 이 세션에서 그렇게 잡은 회귀가 여러 건이다.

**한글은 음절 단위다.** 부분 문자열 매칭에서 `외로`는 `외롭다`를 못 잡고,
`미쳐버리`는 `미쳐버릴`을 못 잡는다. 어간을 줄일 때마다 활용형을 실측한다.
반대로 너무 줄이면(`미처버`) `미처 버리지 못한`을 삼킨다.

**로컬에서 되는 것이 CI에서 안 될 수 있다.** Node 버전, 생성물 존재 여부, gitignore된 파일 의존.
CI 실패는 추측하지 말고 로그를 읽는다 — `gh run view <id> --log-failed`.
(`gh`는 설치·인증돼 있다. 새 셸이면 PATH에 잡히고, 아니면 `"/c/Program Files/GitHub CLI/gh.exe"`)

**필터를 넓힐 때는 반대 방향 피해를 먼저 계산한다.**
설명란까지 훑으면 상담전화를 적은 좋은 채널이 걸리고, `먼저간`을 넣으면 "친구가 먼저 간 카페"가 걸린다.
정상 표현을 넣어보고 걸리는지 확인한 뒤에 넣는다.

**증상이 보고된 위치가 원인의 위치가 아닐 수 있다.**
"워크플로가 목록에 없다" → 실제로는 있었고 5회 실패 중이었다.
"아이콘이 깨졌다" → PNG는 정상이었고 88%가 검정이라 안 보였을 뿐이다.
보고를 전제로 받아들이지 말고 먼저 측정한다.

---

## 7. 새 세션에서 읽을 순서

1. **이 문서** — 지금 무엇이 걸려 있는지
2. **`PLAN.md`** 1~4절 — 목표·제약·아키텍처·데이터 스키마. 5절 Phase 3은 오프라인 정책 명세라 앱을 만질 때 필요
3. **`taxonomy.yaml`** — 감정 체계·검색어·blocklist·위기 정책의 단일 소스.
   주석에 **왜 그 값인지**가 날짜와 함께 적혀 있다. 값만 보고 고치지 말 것
4. **`OPERATIONS.md`** — 운영 절차. 특히 4.5절(채널 차단)과 6절(쿼터)
5. 코드를 만질 때만: `scripts/build_videos.py` 상단 docstring(실행 원칙 5가지) → `scripts/lib/` → `app/src/`

**빠른 확인 명령** (전부 쿼터 0)

```bash
python scripts/normalize_test.py      # 분류 회귀 — label·활용형·위기 우선순위
python scripts/blocklist_test.py      # 채널 차단
python scripts/quota_test.py          # 429/403 판정
python scripts/build_videos.py --dry-run --out-dir /tmp/x
cd app && npm test                    # 49개
```
