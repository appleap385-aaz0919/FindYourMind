# 감정 기반 유튜브 추천 앱 — 개발 계획서 (v14)

## 1. 목표와 제약

**목표**: 사용자가 자신의 마음 상태를 자연어로 입력하면, 감정을 분류해 그에 맞는 유튜브 영상 리스트를 추천하고, 클릭 시 유튜브로 이동시키는 서비스.

**제약 조건 (확정)**
- 운영 시 LLM/외부 AI API 호출 없음 — 감정 분류는 온디바이스(브라우저)에서 수행
- 운영 서버 없음 — 백엔드, 서버 DB, 로그인 인프라 모두 없음
- 비로그인, 누구나 사용
- 데이터는 사용자 디바이스에 경량 저장
- 감정 카테고리는 세분화 (계층형, 최종 16~24개)

**스택 (확정)**
- 웹 기반 PWA (React) → 완성 후 Capacitor로 래핑해 앱스토어 배포
- 온디바이스 추론: TensorFlow.js (분류 모델) + 순수 JS 키워드 사전
- 로컬 저장: IndexedDB
- 호스팅: GitHub Pages (앱 + videos.json 동일 저장소 서빙)
- 개발 워크플로우: UI·분류 로직·화면 흐름은 Claude 아티팩트로 즉시 미리보기하며 검증 → 검증된 코드를 Claude Code로 실제 프로젝트에 조립

## 2. 전체 아키텍처

```
[GitHub Actions (일 1회 cron)]
   └─ YouTube Data API v3 호출 (카테고리별 검색어 세트)
   └─ 결과 정제 → videos.json 생성
   └─ GitHub Pages에 배포 (앱과 같은 저장소)
            │
            ▼ (앱 실행 시 버전 비교 후 변경 시만 fetch, 실패 시 로컬 캐시)
[PWA (React, 스탠드얼론)]
   ├─ 감정 입력: 자연어 텍스트 또는 대분류→세분류 순차 선택
   ├─ 온디바이스 분류: TF.js 모델(대분류) + 키워드 사전(세분류 보정)
   ├─ IndexedDB: videos.json 캐시 + 갱신 시각
   ├─ 영상 리스트 UI (썸네일: img.youtube.com/vi/{videoId}/mqdefault.jpg)
   └─ 클릭 → youtube.com/watch?v={videoId} 새 창/유튜브 앱 열기
            │
            ▼ (완성 후)
[Capacitor 래핑 → iOS/Android 스토어 배포]
```

핵심 원칙: 앱은 YouTube API를 절대 직접 호출하지 않는다. API 키는 GitHub Actions Secrets에만 존재한다.

## 3. 감정 분류 체계 (Phase 0에서 확정)

계층형 2단계. 초안:

| 대분류 (모델이 분류) | 세분류 (키워드 사전으로 보정) |
|---|---|
| 불안 | 초조, 걱정, 긴장 |
| 분노 | 짜증, 억울함, 격분 |
| 답답함 | 막막함, 진전 없음, 억눌림 |
| 우울 | 슬픔, 외로움, 상실감 |
| 지침/무기력 | 번아웃, 피로, 의욕 없음 |
| 기쁨 | 뿌듯함, 즐거움, 감사 |
| 설렘 | 기대, 두근거림 |
| 평온 | 여유, 안정 |
| 심심함/무료함 | 지루함, 새로운 자극 원함 |

- 대분류 9개 × 세분류 2~3개 = 세분류 약 20~24개
- **입력 방식 2가지 모두 제공 (확정)**: (1) 자연어 텍스트 입력 → 온디바이스 분류, (2) 대분류 → 세분류 순차 선택 UI
- 자연어 분류 실패/모호 입력 → 대분류 폴백, 그래도 모호하면 선택 UI로 자연스럽게 유도 (두 방식이 폴백 관계로도 연결됨)
- 각 세분류마다: (a) 키워드 사전 엔트리, (b) 유튜브 검색어 세트 3~5개, (c) 추천 톤 정의 (위로/환기/공감/전환 중 무엇을 지향하는지)

**[감성 터치 원칙]** 문구는 감정을 판단하지 않고 말을 건다. 명령형("입력하세요") 대신 대화형("오늘 마음, 어땠어요?"). 분류 실패도 사용자 탓으로 만들지 않는다. 공감 문장·마무리 문구·플레이스홀더는 모두 복수 후보 중 랜덤 표시하되 직전 문구는 제외한다.

**[중요 원칙] 우울·불안·분노 계열은 감정을 증폭시키는 콘텐츠(슬픈 영상 몰아보기, 분노 유발 콘텐츠 등)가 아니라 완화·환기 방향의 검색어로 설계한다. Phase 0에서 세분류별 검색어를 확정할 때 이 원칙으로 전수 검토한다.**

## 4. 데이터 스키마

**videos.json (정적 배포 산출물)**

위기 카테고리는 `categories` 배열에 넣지 않고 **최상위 `crisis` 객체로 분리**한다.
`categories`를 순회하는 어떤 코드도 위기 영상에 닿을 수 없어, 앱이 실수로 일반 추천 목록에
위기 영상을 섞는 사고가 구조적으로 불가능해진다. (앱의 필터링 기억에 의존하지 않는다.)

```json
{
  "version": "2026-08-12T00:00:00Z",
  "categories": [
    {
      "id": "anxiety.restless",
      "parent": "anxiety",
      "label": "초조함",
      "videos": [
        {
          "videoId": "abc123",
          "title": "...",
          "channel": "...",
          "publishedAt": "...",
          "duration": "PT12M30S"
        }
      ]
    }
  ],
  "crisis": {
    "updated_at": "2026-08-12T00:00:00Z",
    "source": "whitelist+guarded_search",
    "videos": [ { "videoId": "...", "title": "...", "channel": "...",
                  "publishedAt": "...", "duration": "..." } ]
  }
}
```

- `crisis.updated_at`을 따로 두어 위기 풀의 신선도를 독립적으로 확인·경보할 수 있게 한다
  (직전 결과 유지 규칙이 발동하면 이 값만 오래된 상태로 남는다)
- **빌드 시점 검증(필수)**: `crisis.videos`의 videoId가 `categories` 어디에도 없고 그 역도 성립하는지
  단언(assert)한다. 위반 시 배포하지 않는다.
- **crisis.videos와 categories[].videos는 동일한 5개 필드를 쓴다** (publishedAt 포함).
  v9 예시의 4개 필드는 축약 표기였으며, 신선도 정렬·감사 추적에 publishedAt이 필요하다.
- 위기 상황의 안내 문구·상담처·마무리 문구는 taxonomy.yaml에서 앱 번들로 들어가므로
  videos.json에는 영상만 담는다 (문구와 데이터의 소스를 섞지 않는다).
- 카테고리당 영상 15~20개, 전체 예상 크기 300~500KB
- 썸네일 URL은 저장하지 않음 (videoId로 조립)

**IndexedDB (idb 라이브러리 사용)**
- `videos_cache`: videos.json 원본 + 수신 시각
- `settings`: 마지막 갱신 시각, 선호 세분류(선택적, 전부 온디바이스)

## 5. 단계별 작업 계획

### Phase 0 — 감정 체계·검색어 설계 (코딩 전, 채팅으로)
- [ ] 대분류/세분류 최종 확정
- [ ] 세분류별 키워드 사전 초안 (구어체·신조어 포함, 세분류당 20~40개)
- [ ] 세분류별 유튜브 검색어 세트 확정 — 완화·환기 원칙 전수 검토
- 산출물: `taxonomy.yaml` (배치·앱이 공유하는 단일 소스)

### Phase 0.5 — UI 프로토타입 (Claude 아티팩트, 채팅으로)
- [x] 입력(자연어 + 대분류→세분류 선택 2가지 방식) → 분류 결과 → 영상 리스트 전체 흐름을 React 아티팩트로 제작
- [x] 샘플 데이터 내장, 키워드 사전 분류 로직 탑재 — emotion-prototype.jsx의 normalize/classify를 Phase 3에 그대로 이식
- [x] **로딩 지연 확정 → 1000ms** (즉답 < 800ms < 1000ms 순으로 비교). Phase 3에서 min_duration_ms=1000으로 구현
- [x] UI 문구 온도 검수 완료 — 현재 톤 유지 확정
- [x] 비주얼 방향 확정: 자정 청록-자주 그라데이션 + 명조체 공감 문장 + 호흡하는 빛(10초 주기)
- [ ] 미리보기로 UX 반복 수정
- 참고: 아티팩트 환경 제약상 외부 fetch·IndexedDB는 미리보기에서 메모리 대체 → Phase 3에서 실제 구현으로 교체

### Phase 1 — 데이터 파이프라인 (Claude Code)
- [ ] Python 배치: taxonomy.yaml → search API → 중복 제거·Shorts/저품질 필터 → videos.json
- [ ] videos API로 삭제·비공개 영상 검증 (1유닛/건이라 저렴)
- [ ] GitHub Actions: 일 1회 cron, API 키는 Secrets, gh-pages 배포
- [x] **배포 경로 확정**: 데이터는 `data/` 하위에 배포한다
      - `data/version.json` (수십 바이트: version, crisis.updated_at만)
      - `data/videos.json` (본체 300~500KB)
      - `data/channel_health.json`
      - 앱은 루트에 배포. 데이터·앱 워크플로 **양쪽 모두** `keep_files: true`
      - 앱이 매번 500KB를 받지 않도록: 먼저 version.json을 받아 비교하고,
        변경된 경우에만 videos.json을 받는다 (GitHub Pages는 캐시 헤더 제어가 안 되므로
        fetch 시 `?v={version}` 쿼리로 캐시 무효화)
      - 두 워크플로가 같은 브랜치에 쓰므로 Actions `concurrency` 그룹으로 동시 배포 방지
      - **배포 후 검증**: 앱 배포 워크플로 마지막에 `data/videos.json` 존재 여부를 확인하고
        없으면 실패 처리 (keep_files 설정이 깨진 걸 조용히 넘기지 않는다)
- [x] **쿼터 재산정 (정정)**: 초기 계산 9,600유닛은 위기 카테고리·검증·헬스체크를 누락한 값이었다.
      실제 전량 실행 시 ≈10,358유닛으로 한도(10,000)를 초과한다.
- [x] **해결책 확정 — 쿼리 3/4 로테이션 + 하드캡**
      - 세분류별 검색어 4개 중 매일 3개만 사용, 날짜 기반으로 회전 → 7,200유닛
      - 세분류마다 스킵 인덱스를 다르게 둔다: `skip = (day_of_year + subcategory_index) % 4`
        (전 세분류가 같은 쿼리를 동시에 빠뜨리지 않도록)
      - 이전 결과와 merge하므로 영상 풀 다양성 손실 없음. 오히려 날마다 쿼리 조합이 달라져 장기 다양성은 증가
      - 위기 카테고리는 전량 유지(전용 쿼리 6 + 화이트리스트) — 안전 장치는 절감 대상이 아님
      - 합계 ≈7,950유닛, 여유 약 2,000유닛
- [x] **하드캡 9,800유닛**: 실행 전 예상 소모량을 계산해 초과 시 중단
- [x] **실행 순서 고정**: 위기 카테고리를 가장 먼저 처리한다. 중단이 발생해도 안전 관련 데이터는 이미 갱신된 상태가 되도록
- [x] **중단 시 원자성**: 하드캡·API 오류로 중단되면 부분 결과를 쓰지 않고 직전 videos.json을 그대로 유지한다 (부분 배포 금지)
- [x] **화이트리스트 수집 경로**: `search.list(channelId)`는 채널당 100유닛이라 사용 금지.
      `channels.list` → uploads 플레이리스트 → `playlistItems.list` 경로로 채널당 1유닛
- [x] **배치 실행 시각**: 쿼터는 태평양 시간 자정에 리셋되므로 리셋 직후 실행. 실패 시 자동 재시도는 1회로 제한(이중 소모 방지)
- [ ] **위기 카테고리 채널 화이트리스트 초기 구축** — `channel_allowlist.yaml` 생성, 채널 15~25개 선정 (선정 기준은 taxonomy.yaml의 selection_criteria 참조)
- [ ] **화이트리스트 자동 점검 로직**: 배치 실행 시 각 채널의 (1) 존재 여부, (2) 최근 90일 업로드 유무, (3) 최근 영상이 blocklist 필터를 통과하는 비율을 계산해 `channel_health.json`으로 출력
- [ ] **이상 감지 시 GitHub Issue 자동 생성**: 채널 삭제·90일 무업로드·필터 통과율 50% 미만이면 Actions가 Issue를 열어 담당자에게 알림 (사람이 잊어도 시스템이 먼저 말한다)
- [x] **Phase 1 구현 중 확정된 운영 상수** (PLAN에 근거 수치가 없어 구현 시 정한 값, 전부 승인됨)
      - 위기 풀 상한 20건 (하한 12는 taxonomy 근거). 화면 노출은 이 풀에서 매번 4~6개 랜덤
      - crisis_stale 경보 임계 3일
      - 동일 Issue 재알림 주기: **안전 관련 경보(crisis_stale, 화이트리스트 고갈)는 2일, 그 외는 7일**
      - check_channels 하드캡 500유닛
      - build_report.json 산출 — 진단용. 공개 배포에서는 제외하고 Actions 아티팩트로만 보관
- [x] **crisis 제외 후 보충 규칙**: 위기 풀에 포함된 videoId는 일반 카테고리 후보 단계에서 제외하고
      다음 순위로 채운다. 제외로 카테고리 확보량이 줄지 않게 한다. 카테고리별 제외 건수를 로그·리포트에 기록
- 완료 기준: 매일 갱신되는 videos.json이 공개 URL에서 수신됨 + channel_health.json이 함께 배포됨

### Phase 2 — 온디바이스 감정 분류 모델 (Claude Code)
- [ ] AI Hub 감성 대화 말뭉치 확보 → 대분류 9개 라벨 매핑 (답답함·심심함은 말뭉치 라벨에 없을 수 있어 키워드 기반 재라벨링 또는 데이터 보강 필요)
- [ ] 후보 학습·비교: (a) fastText 양자화 → TF.js 변환 가능성 확인, (b) 소형 Transformer → TF.js 변환·양자화
- [ ] 브라우저 로딩 크기 목표: 모델 10MB 이하 (초기 로딩 UX 고려), 미달성 시 사전 단독 + 점진 로딩 검토
- [ ] 평가: held-out 정확도 + 구어체 실사용 문장 100개 수동 테스트셋. 대분류 80% 이상 채택
- [ ] 세분류 보정: 대분류 내 키워드 사전 매칭, 무매칭 시 대분류 폴백
- 산출물: TF.js 모델 + 라벨 매핑 + 검증 스크립트

### Phase 3 — PWA 조립 (Claude Code)
- [ ] React 프로젝트 셋업 (Vite), idb, TF.js
- [ ] Phase 0.5 아티팩트 코드 이식
- [ ] **데이터 갱신·오프라인 정책** (아래 명세대로 구현)

  **갱신 시점** — PWA에는 크론이 없다. "매일 0시"처럼 정해진 시각에 도는 게 아니라,
  사용자가 앱을 열 때 확인한다.
  - 앱 실행(포그라운드 진입) 시 마지막 확인 시각이 6시간을 넘었으면 version.json 확인
  - 확인·다운로드는 **UI를 막지 않는다**. 화면은 캐시로 즉시 그리고, 갱신은 뒤에서 진행
  - 갱신이 끝나도 보고 있는 화면을 바꾸지 않는다. 다음 입력부터 새 데이터 사용
  - service worker의 주기적 백그라운드 동기화는 지원 브라우저가 제한적이므로 의존하지 않는다
    (지원되면 보너스로 동작하는 정도)

  **오프라인·실패 시나리오 (전부 조용히 처리, 오류 화면 금지)**

  | 상황 | 동작 |
  |---|---|
  | 최초 실행 + 온라인 | 번들 시드로 즉시 시작 → 뒤에서 최신본 받아 교체 |
  | 최초 실행 + 오프라인 | 번들 시드로 정상 동작. 오류 표시 없음 |
  | 재방문 + 변경 없음 | version.json(수십 바이트)만 받고 종료 |
  | 재방문 + 변경 있음 | 본체 다운로드 → **완전히 받은 뒤에만** 캐시 교체(원자적) |
  | 재방문 + 오프라인 | 캐시로 정상 동작. 오류 표시 없음 |
  | 다운로드 중 끊김 | 부분 데이터 폐기, 기존 캐시 유지 |
  | 캐시 30일 초과 | YouTube API 약관상 30일 내 갱신 의무 → 캐시 폐기하고 번들 시드로 복귀 |
  | 영상이 삭제됨(썸네일 404) | 해당 카드를 조용히 숨김. 빈 자리 남기지 않음 |
  | 유튜브 앱/브라우저 열기 실패 | 웹 URL로 재시도 |

  **오프라인에서 실제로 되는 것과 안 되는 것 (정확히)**

  | 기능 | 오프라인 | 비고 |
  |---|---|---|
  | 감정 입력 → 분류 | 동작 | 전부 온디바이스 |
  | 공감 문장·마무리 문구 | 동작 | 앱 번들에 포함 |
  | 위기 감지 + 상담 전화번호 | 동작 | 전화 걸기는 통화망이므로 데이터 없이도 가능 |
  | 영상 제목·채널명 표시 | 동작 | 캐시된 메타데이터 |
  | 영상 썸네일 | 조건부 | service worker가 캐시한 것만. 처음 보는 영상은 표시 불가 |
  | **영상 재생(유튜브 이동)** | **불가** | 유튜브 자체가 네트워크를 요구 |

  → 오프라인에서 "정상 동작"한다고 말하면 과장이다. **감정을 받아주는 부분은 온전히 동작하지만
  영상은 열리지 않는다.** 이 상태를 설계에 반영한다.

  **오프라인 대응 설계**
  1. 썸네일 캐싱: service worker가 `img.youtube.com` 응답을 cache-first로 저장 (최근 200개, LRU).
     한 번 본 영상은 오프라인에서도 목록이 온전히 보인다.
  2. 영상 영역 문구 교체: 오프라인이면 영상 카드를 흐리게 처리하고
     "지금은 연결이 없어 영상을 열 수 없어요. 목록은 저장해둘게요"를 조용히 표시한다.
     탭했는데 아무 일도 안 일어나는 것보다 미리 말해주는 편이 낫다.
  3. **오프라인 위로 경로(네트워크 불필요) — [확정] 오프라인 전용**
     영상을 열 수 없을 때만 제시한다. 온라인 상태에서는 어떤 카테고리에서도 노출하지 않는다.
     (영상 추천이 이 서비스의 본 기능이고, 대체재가 상시 노출되면 본 기능이 흐려진다)
     - 호흡 가이드: 들이쉬기 4초 / 멈춤 7초 / 내쉬기 8초, 화면의 호흡하는 빛과 연동
     - 짧은 이완 안내 문구 (어깨 내리기, 시선 멀리 두기 등)
     - 전부 텍스트·애니메이션이라 용량 부담 없음
     - 노출 조건: 오프라인 감지 시에만. 온라인 복귀하면 다음 화면부터 사라진다
     - 문구는 "영상 대신"이라는 표현을 쓰지 않는다. 아쉬움을 강조하지 않고
       그 자체로 완결된 제안처럼 보이게 한다 (예: "잠시 같이 숨을 골라볼까요")

  **원칙**: 배경 갱신 실패는 사용자에게 알리지 않는다("연결 실패" 배너 금지).
  다만 사용자가 곧 시도할 동작(영상 열기)이 불가능한 경우에는 미리, 조용히 알린다.
  침묵이 배려인 지점과 불친절이 되는 지점을 구분한다.
  갱신 상태는 설정 화면에서만 확인할 수 있게 한다 (마지막 갱신 시각 표시).
- [ ] TF.js 모델 로딩·추론 연결 (Web Worker로 UI 블로킹 방지)
- [ ] PWA 설정: manifest, service worker (오프라인 동작, 최초 실행용 시드 JSON 번들)
- [ ] GitHub Pages 배포
- 완료 기준: 배포 URL에서 입력→추천→유튜브 이동 전체 흐름 동작, 오프라인(캐시 존재 시) 동작

### Phase 4 — 검증·정리
- [ ] 삭제 영상 필터 1주 운영 관찰
- [ ] 오분류 사례 수집 → 사전/데이터 보강 1회전
- [ ] YouTube API 약관 점검: 캐시 30일 내 갱신(일 배치로 충족), 저장 항목 허용 범위 확인
- [ ] 완화·환기 원칙 실검수: 우울·불안 카테고리 실제 추천 결과 전수 확인

### Phase 4.5 — 운영 절차 확정 (배포 전 반드시 완료)

이 서비스는 배포 후 방치되면 위험해지는 지점이 있다. 담당자와 주기를 문서에 명시하고,
가능한 점검은 전부 자동화해 사람의 기억에 의존하지 않게 한다.

**정기 운영 체크리스트 (`OPERATIONS.md`로 저장소에 커밋)**

| 주기 | 항목 | 방법 | 담당 |
|---|---|---|---|
| 매일(자동) | 배치 성공 여부 | Actions 실패 시 알림 | 시스템 |
| 매일(자동) | 채널 생존·업로드·필터 통과율 | channel_health.json 생성, 이상 시 Issue 자동 생성 | 시스템 |
| 월 1회 | 위기 카테고리 영상 샘플 점검 | 실제 노출 영상 10개를 사람이 직접 열어 확인 | 운영자 |
| 월 1회 | 자동 생성된 Issue 처리 | 채널 교체·제외 후 channel_allowlist.yaml 수정 | 운영자 |
| 분기 1회 | 화이트리스트 전체 재검토 | 채널별 최근 영상 성향 변화 확인, 필요 시 신규 채널 보충 | 운영자 |
| 분기 1회 | blocklist_terms 갱신 | 신조어·새로운 위험 표현 반영 | 운영자 |
| 반기 1회 | 감정 키워드 사전 보강 | 오분류 사례 반영 | 운영자 |

**화이트리스트 관리 절차 (누락 방지 장치)**
1. 채널은 `channel_allowlist.yaml` 한 파일에서만 관리한다 (다른 곳에 흩어두지 않는다)
2. 채널 추가·제외는 PR로만 한다 — 변경 이력이 남고 누가 왜 바꿨는지 추적된다
3. 각 채널 항목에 `added_at`, `last_reviewed_at`, `reviewed_by`를 필수로 기록한다
4. 배치가 `last_reviewed_at`이 120일을 넘긴 채널을 발견하면 Issue를 자동 생성한다
5. 화이트리스트가 15개 미만으로 줄면 배치가 경고 Issue를 생성한다 (풀 고갈 방지)
6. 인수인계 시 OPERATIONS.md 하나만 넘기면 되도록 모든 절차를 이 문서에 모은다

**최소 안전선**: 운영자가 장기간 부재해도, 배치는 blocklist 필터와 "12개 미달 시 직전 결과 유지"
규칙 덕분에 위험한 콘텐츠를 새로 노출하지 않는다. 방치의 결과는 신선도 저하이지 안전성 저하가 아니다.

### Phase 5 — 앱 전환 (Capacitor)
- [ ] Capacitor 래핑, 유튜브 딥링크(앱 우선 열기) 네이티브 처리
- [ ] iOS/Android 빌드·스토어 심사 준비
- [ ] 개인정보처리방침 (수집 정보 없음 명시)

## 6. 트레이드오프 기록

| 결정 | 대안 | 선택 이유 |
|---|---|---|
| 정적 JSON + Actions 배치 | 앱 직접 API 호출 | 키 노출·프로젝트 단위 쿼터 공유로 대안은 실사용 불가 |
| 웹 PWA + Capacitor | Flutter | 개발 중 아티팩트 즉시 미리보기 가능, 웹→앱 전환이 재작성 없는 래핑, 호스팅까지 GitHub Pages로 통일. 대가: TF.js 모델 크기·초기 로딩 관리 필요 |
| 대분류 모델 + 세분류 사전 | 세분류 직접 분류 모델 | 세분류 직접 학습은 클래스당 데이터 부족으로 정확도 붕괴 위험 |
| IndexedDB | localStorage | 수백 KB JSON 저장에 용량·구조 모두 적합. 아티팩트 미리보기에선 메모리 대체 |

## 7. 확장 시 재검토 지점
- 검색어 세트 대폭 확장 시 쿼터 초과 → 분할 실행 또는 복수 프로젝트 키 순환
- 개인화(자주 선택한 감정 가중치)는 전부 온디바이스 처리 — 서버 불필요 유지
- Capacitor 전환 시 iOS 심사: 외부 링크 방식은 문제없음. 임베드 재생으로 바꿀 경우 YouTube iframe 정책 별도 확인
- PWA 단계에서 이미 설치형처럼 쓸 수 있으므로(홈 화면 추가), 스토어 배포 필요성 자체를 사용 반응 보고 재판단 가능
